# Python
__pycache__/
*.pyc
backend/venv/
backend/.pytest_cache/
*.egg-info/
.coverage
htmlcov/

# data/raw_notes/ IS tracked — it currently holds synthetic sample notes
# committed as demo/portfolio data (see README). If you swap in private
# real notes later and don't want them public, either .gitignore the
# specific files you add or ask to split sample vs. private note dirs.

# Chunker output is regenerated by scripts/run_ingestion.py — not
# something to version.
backend/data/processed/**
!backend/data/processed/.gitkeep

# ChromaDB persistence (added in Stage 2)
backend/data/chroma/

# Env files
.env
.env.local
backend/.env

# Node / frontend (added in Stage 5)
frontend/node_modules/
frontend/dist/
frontend/build/

# OS/editor cruft
.DS_Store
*.swp
